services:
  rabbitmq:
    image: rabbitmq:management
    container_name: rabbitmq
    environment:
      - RABBITMQ_DEFAULT_USER=kira
      - RABBITMQ_DEFAULT_PASS=kira
    ports:
      - "5672:5672"
      - "15672:15672"

networks:
  default:
    driver: bridge
